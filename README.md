# cursor_test

## Daily test runs

This repository contains Playwright smoke tests and a GitHub Actions workflow (`.github/workflows/daily-tests.yml`)
that is intended to run on a schedule and email a short report.

Note: GitHub can sometimes delay or temporarily stop scheduled workflow triggers; making periodic repository
updates can help ensure scheduled workflows remain active.
